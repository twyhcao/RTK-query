##This is a repo showing the difference between Redux-saga and RTK query

**Start redux-saga-app**
`cd redux-saga-app/
npm run start`
The Redux saga app will start at `http://localhost:3001/`

**Start rtk-query-app**
`cd rtk-app/
npm run start`
The Redux saga app will start at `http://localhost:3002/`